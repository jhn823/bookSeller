<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel='stylesheet' href='/stylesheets/search.css' />
    <title>Document</title>
</head>
<body>
    <h1>구독내역조회</h1>
    <hr>

    <table id="table" > 
          <p>count  <%= print[1][0].count %></p>  
        <thead>  
            <tr>  
                <th>구매ID</th>  
                <th>UserId</th>  
                <th>From</th>  
                <th>To</th>  
                <th>Amount</th>  
            </tr>  
        </thead>  
         <tbody>  
         <% print[0].forEach(function(result) { %>
            <tr>  
                <td><%= result.buy_id %></td>  
                <td><%= result.user_index %></td>
                <td><%= result.from_date.toISOString().replace(/T/, ' ').replace(/\..+/, '') %></td>
                <td><%= result.to_date.toISOString().replace(/T/, ' ').replace(/\..+/, '') %></td>
                <td><%= result.amount %></td>
            </tr>                                   
         <% }) %>
         </tbody>
    </table>  
</body>
</html>